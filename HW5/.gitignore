# Ignore node_modules directories at the root and within any first-level subdirectory
/node_modules
/*/node_modules

# Ignore package-lock.json at the root and within any first-level subdirectory
/package-lock.json
/*/package-lock.json

# Ignore yarn.lock at the root and within any first-level subdirectory (if using Yarn)
/yarn.lock
/*/yarn.lock

# Ignore .env files at the root and within any first-level subdirectory (common for environment variables)
/.env
/*/.env

# Ignore build artifacts that may be created within subdirectories (e.g., dist, build)
/*/dist
/*/build
